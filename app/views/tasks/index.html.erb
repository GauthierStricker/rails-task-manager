<h1> My tasks </h1>
<ul>
  <% @tasks.each do |task| %>
    <li> <%= task.completed === true ? "☑" : "☐" %> - <%= link_to "#{task.title}", task_path(task) %> <%= link_to "Delete", destroy_path(task) %></li>
    <% end %>
    <%= link_to " + Create a new task", tasks_new_path %>
</ul>
